import clsx from 'clsx';

export const hoverClassName = clsx(
	'transform-gpu transition-all duration-500 will-change-[border,_transform] hover:scale-95 active:scale-100',
	'border-0 hover:border-4 border-transparent hover:border-white/75 dark:hover:white/10',
);
